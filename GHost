
public class PinkGhost extends Actor
{
    private String img;
    private GifImage gif;
    private boolean left;
    private boolean right;
    public PinkGhost()
    {
        this.img = img;
        gif = new GifImage("PINKKghost.gif");
        int scalePercent = 50;
        

    }

    public void act() 
    {
        setImage( gif.getCurrentImage() );
        move(2);
        Actor a = getOneObjectAtOffset(20,0,Wand.class);
        if (a!=null){ 
        int x = Greenfoot.getRandomNumber(3);
       
        if (x == 1) {this.setRotation(180); move(2);}
        if(x == 2) {this.setRotation(270); move (2);}
        if (x == 3) {this.setRotation(90); move(2);}
    }
         
         
        
        }
        }   
